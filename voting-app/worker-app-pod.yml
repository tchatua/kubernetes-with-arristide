apiVersion: v1
king: Pod
metadata:
  name: worker-app-pod
  labels:
    name: worker-app-pod
    app: voting-app
spec:
  containers:
    - name: worker-app
      image: kodekloud/examplevotingapp_worker